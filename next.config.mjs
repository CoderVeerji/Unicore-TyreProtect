/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  transpilePackages: ['three'],
  reactStrictMode: false,
};

export default nextConfig;
